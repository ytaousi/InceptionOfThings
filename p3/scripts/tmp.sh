#!/bin/bash
# if ! command -v <command> 1>/dev/null
# then
#     echo "<command> could not be found"
# else
#     echo "<command> is already installed"
# fi

#kubectl wait --for=condition=ready pod -l name=application1